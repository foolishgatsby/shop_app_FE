export class User {
  // constructor
  constructor(email, password, fullname, phone_number, address, date_of_birth) {
    this.email = email;
    this.password = password;
    this.fullname = fullname;
    this.phone_number = phone_number;
    this.address = address;
    this.date_of_birth = date_of_birth;
  }
}
